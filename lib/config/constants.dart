class Constants {
  static const API_URL = 'https://homekeeper-backend.herokuapp.com/graphql/';
  static const SYNC_DURATION = const Duration(minutes: 1);
  static const TIMEOUT = const Duration(seconds: 8);
}
