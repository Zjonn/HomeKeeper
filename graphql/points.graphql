query teamMembersPoints($teamId: Int!, $fromDateTime: DateTime!, $toDateTime: DateTime!) {
    teamMembersPoints(teamId: $teamId, fromDatetime: $fromDateTime, toDatetime: $toDateTime) {
        member {id},
        points
    }
}
